# Triton loads versioned model dirs. Copy the Rung-4 engine here as model.plan:
#   cp ../../../artifacts/dinov2_trt_fp16.plan model.plan
# (the .plan is large + GPU/TRT-version specific, so it's gitignored — see ../../README.md)
